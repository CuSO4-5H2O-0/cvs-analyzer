# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Python 项目，当前为初始状态，尚无实际代码。

## Environment

- **运行环境**: Windows 11 (Git Bash)
- **Python**: 通过 Anaconda 管理
- **Git**: v2.54.0，已绑定 GitHub 账号 `CuSO4-5H2O-0`

## Commands

```bash
# Python 运行
python <file.py>

# Git 操作
git add <file>
git commit -m "message"
git push
```

## Project Structure

```
firsy_cc/
├── .claude/
│   └── settings.local.json   # 本地权限配置
├── CLAUDE.md                  # 本文件
└── ...
```
